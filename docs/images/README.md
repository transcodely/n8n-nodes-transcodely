# Screenshots to capture

The package README has no screenshots yet, and deliberately references none: a
link to a file that is not in the repository renders as a broken image on npm
and on GitHub. Capture these five from a real n8n instance, drop them in this
folder under these names, and add them to the README before submitting the
package for n8n verification.

| File | What to capture |
|---|---|
| `credentials.png` | The Transcodely API credential form, filled in, with the key masked |
| `create-job.png` | The Create Job parameter panel with one HLS output row |
| `wait-for-job.png` | Wait for Completion wired after Create Job on the canvas |
| `create-video.png` | The Create Video From URL parameter panel |
| `trigger.png` | The Transcodely Trigger with its event selector open |

Mask every real key, app id and hostname before committing an image.
